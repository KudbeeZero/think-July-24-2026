# KILO Agent 2027 Roadmap: Next Steps for KUDBEE Project

This document outlines the high-priority strategic initiatives and engineering items KILO should tackle to continue bridging the gap between the `think-July-24-2026` architecture and the KUDBEE production monorepo.

## 1. Upstream OpenCode Interceptor (`// kilocode_change`)
- **Objective**: Implement the // kilocode_change annotation pattern. 
- **Context**: The July-24 architecture specifies this pattern for the trajectory interceptor, but it requires modifying the upstream OpenCode core which currently isn't part of the KILO monorepo.
- **Action**: Propose and merge changes to the upstream packages/opencode repository to expose telemetry hooks.

## 2. Model Context Protocol (MCP) Server Implementation
- **Objective**: Introduce a robust MCP server.
- **Context**: While `services/mcp_server.ts` exists, it needs full alignment with the July-24 architecture to support cross-tool routing.
- **Action**: Flesh out the endpoints and memory capabilities required by the MCP schema.

## 3. Challenge/Disruptor Tokens (Chaos Engineering Layer)
- **Objective**: Formalize the disruptor tokens for chaos engineering.
- **Context**: Current `/api/grok/ask` routing introduces disruptor token latency logic, but it's not robustly exposed via the UI or the EngineBus.
- **Action**: Build a complete visual control plane in the KILO dashboard for triggering specific chaos engineering events.

## 4. Refactoring and Consolidating LLM Routing
- **Objective**: Expand the newly created `services/lib/llmRouter.ts`.
- **Context**: The multi-tier fallback (xAI → proxy → Groq → DeepSeek → Gemini → sim) is critical for system resilience.
- **Action**: Augment this router to directly emit detailed ECP hit/miss logs so that token caching metrics accurately track multi-tier fallback events.

## 5. Completing the Vector Memory Pipeline
- **Objective**: Ensure the `kudbee_recall_memories` stub actively uses `pgvector`.
- **Context**: We successfully wired `kudbee_recall_memories` to `services/memory/vectorStore.ts`.
- **Action**: Implement the actual PostgreSQL/pgvector queries utilizing `Drizzle ORM` to retrieve the relevant think-tokens.

---
*Generated by KILO Agent*
